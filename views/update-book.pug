extends layout 

block content 
    h1= title 
    form(action=`/books/${book.id}` method="post")
        include form
        p 
            input(type="submit" value="Update Book")
        p: a(href="/books" class="button") Cancel 